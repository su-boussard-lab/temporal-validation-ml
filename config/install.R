# List of R packages required for this project
packages.install <- c(
    "tidyverse",
    "ggplot2",
    "lubridate",
    "viridis",
    "RColorBrewer",
    "tsibble",
    "tidyr",
    "ggridges",
)

# Install packages
install.packages(packages.install)
